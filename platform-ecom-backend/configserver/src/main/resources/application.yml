server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ViMinhThang/platform-ecom
          default-label: config-server
          timeout: 30
          clone-on-start: true
          force-pull: true