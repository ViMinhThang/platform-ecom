spring:
  application:
    name: product-service
  config:
    import: "configserver:http://localhost:8888"
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

file:
  upload-dir: ./uploads
